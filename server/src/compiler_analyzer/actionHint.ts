/**
 * This is first passed as metadata for Diagnostic hints.
 * Then, in the Code Action Provider, this hint is used to resolve actions.
 * This is number-based to transfer quickly between server and client.
 */
export enum ActionHint {
    // TODO Remove?
}
